{% extends 'base.home.html.twig' %}


{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block header_content %}

  {{ parent() }}
  
{% endblock header_content %}



{% block layout_content %}
  <section class="section-hero overlay inner-page bg-image" style="background-image: url('images/hero_1.jpg');" id="home-section">
      <div class="container">
        <div class="text-center text-capitalize md-7">
          <h1 class="text-white font-weight-bold">Candidatez</h1>
        </div>
      </div>
  </section>

  
  <section class="site-section">
    <div class="container">
          {# templates/base.html.twig #}
          {% for message in app.flashes('success') %}
      
          <div class="alert alert-success">
              {{ message }}
          </div>
    
      {% endfor %}

    

      <div class="row align-items-center mb-5">
        <div class="col-lg-8 mb-4 mb-lg-0">
          <div class="d-flex align-items-center">
            <div>
              <h2>Déposez votre candidature</h2>
            </div>
          </div>
        </div>
        

      </div>

      

      
      <div class="row mb-5">
        <div class="col-lg-12">
          {{form(form)}}

        </div>

       
      </div>
      {# <div class="row align-items-center mb-5">
        
        <div class="col-lg-4 ml-auto">
          <div class="row">
            <div class="col-6">
              <a href="#" class="btn btn-block btn-primary btn-md">Submit</a>
            </div>


          </div>
        </div> #}
      </div>
    </div>
  </section>

{% endblock layout_content %}

{% block javascripts %}
    {{ parent() }}

    <script>
      document.addEventListener("DOMContentLoaded", function() {
          // Select all flash messages
          var flashMessages = document.querySelectorAll('.alert');
  
          // Set a timeout to remove flash messages after 5 seconds (5000 ms)
          flashMessages.forEach(function(message) {
              setTimeout(function() {
                  message.style.display = 'none';
              }, 5000); // Adjust the timeout duration as needed
          });
      });
    </script>


{% endblock %}




