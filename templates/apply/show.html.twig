{% extends 'base.html.twig' %}

{% block title %}Apply{% endblock %}

{% block body %}
    <h1>Apply</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ apply.id }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ apply.reference }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_apply_index') }}">back to list</a>

    <a href="{{ path('app_apply_edit', {'id': apply.id}) }}">edit</a>

    {{ include('apply/_delete_form.html.twig') }}
{% endblock %}
